#include <iostream>



int main(int argc, char** argv) {
  /* Not possible, because:
   *    The pointer only point to the address in memory,
   *    As only if the memeory exists, the address exists,
   *    You can never know the address is valid unless 
   *    the address is referenced.
   */
  
  return 0;
}



