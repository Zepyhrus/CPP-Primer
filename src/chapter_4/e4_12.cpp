#include <iostream>
#include <vector>

using namespace std;

int main()
{ 
  
  int i = 4, j = 3, k = 2;

  if (i != j < k)
    cout << "True" << endl;
  
  cout << "if i != j, if k > 1: true, else: false" << endl;
  cout << "if i == j, if k > 0: true, else: false" << endl;

  return 0;
}
