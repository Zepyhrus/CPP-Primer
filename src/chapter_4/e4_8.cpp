#include <iostream>
#include <vector>

using namespace std;

int main()
{ 
  
  cout << "* The right side of an && is evalutaed if and only if the "  << endl;
  cout << "left side is true;"                                          << endl;
  cout << "* The right side of an || is evaluated if and only if the "  << endl;
  cout << "left side is false;"                                         << endl;
  cout << "Always evaluate left result before right result."            << endl;

  return 0;
}
