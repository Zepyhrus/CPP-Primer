#include <iostream>
#include <vector>

using namespace std;

int main()
{
  cout << "I consider this to be a fair-enough trade-off."              << endl;
  cout << "There is little chance that the order of the evaluations in" << endl;
  cout << "   one single expression matters."                           << endl;
  cout << "And even when we draws the benefits from the order of the "  << endl;
  cout << "   evaluation, it could also easily achieved from seperati-" << endl;
  cout << "   ng the expression into multiple lines or grouping them. " << endl;

  return 0;
}
